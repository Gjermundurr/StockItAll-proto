Program; User creates a new stock item by using client-side GUI interface and enters description and amount. the server recieves the data and creates the new object. The users can then manage their stock inventory by accessing a CLI-menu 
Objective; Keep track of individual stock items using a code, a description and a number of items on hand. 


Classes:
- StockItem: Maintaining individual stock items
	var = description, code, amount
	

- StockTracker(StockItem): Maintaining all the stock items held by StockItAll



### Actions ###
Main.py:
	display CLI menu
	accessing the StockTracker
	storing and retrieving the StockTracker
	performing threading
	running a server

Client.py:
	creating a GUI interface
	capturing a StockItem's code, description and amount
	acting as a client to the server
	displaying a messagebox
